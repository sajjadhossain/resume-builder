var path = __dirname,
    archive = path + '/archive',
    dist = path + '/dist',
    src = path + '/src',
    test = path + '/test',
    vendor = path + '/vendor',
    wiki = path + '/wiki';

module.exports = {
    path: path,
    archive : archive,
    dist: dist,
    src: src,
    test: test,
    vendor: vendor,
    wiki: wiki
};
